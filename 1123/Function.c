#include <stdio.h>
int Watt(int a);
int Volt(int q, int r);


int main(void) {
    int a;
    int b;
    b = Watt(a);
    b = Volt(a, b);
}

int Watt(int a) {
    int w;
    w = Volt(w, 10);
    return w;
}

int Volt(int q, int r) {
    int k;
    int m;
    return k;
}